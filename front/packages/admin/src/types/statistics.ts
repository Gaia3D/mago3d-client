export type SelectedYearStateType = {
  from: string;
  to: string;
};

export type SelectedDateType = {
  from: string;
  to: string;
};

export type IframeBoxPropsType = {
  id: string;
  width: string | number;
  height: string | number;
  src: string;
};
